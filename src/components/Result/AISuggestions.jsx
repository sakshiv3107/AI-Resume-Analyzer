import React from "react";
import {
  Sparkles,
  ArrowRight,
} from "lucide-react";

function AISuggestions({ suggestions = [] }) {
  console.log("AISuggestions:", suggestions);
  return (
    <div className="bg-white rounded-3xl border border-gray-200 shadow-sm p-8 h-full">

      {/* Heading */}
      <h2 className="text-2xl font-bold">
        AI Suggestions
      </h2>

      <p className="text-gray-500 mt-2">
        Personalized recommendations generated by AI to improve your resume.
      </p>

      <div className="mt-8 space-y-5">

        {suggestions.length > 0 ? (
  suggestions.map((suggestion, index) => (
    <div
      key={index}
      className="border border-gray-200 rounded-2xl p-5 hover:border-blue-300 hover:shadow-md transition"
    >
      <div className="flex justify-between items-start">

        <div className="flex gap-4">

          <div className="w-23 h-10 rounded-xl bg-blue-100 text-blue-600 flex items-center justify-center">
            <Sparkles size={20} />
          </div>

          <div>

            <h3 className="font-semibold text-lg">
              {suggestion.title}
            </h3>

            <p className="text-gray-500 mt-2">
              {suggestion.description}
            </p>

          </div>

        </div>

        <span
          className={`px-3 py-1 rounded-full text-xs font-semibold
          ${
            suggestion.priority === "High"
              ? "bg-red-100 text-red-600"
              : suggestion.priority === "Medium"
              ? "bg-yellow-100 text-yellow-700"
              : "bg-green-100 text-green-600"
          }`}
        >
          {suggestion.priority}
        </span>

      </div>
    </div>
  ))
        ) : (
          <div className="border border-dashed border-gray-300 rounded-2xl p-8 text-center">

            <Sparkles
              className="mx-auto text-gray-400"
              size={42}
            />

            <h3 className="font-semibold text-lg mt-4">
              No Suggestions Available
            </h3>

            <p className="text-gray-500 mt-2">
              Great! The AI didn't identify any major improvements for your resume.
            </p>

          </div>
        )}

      </div>

    </div>
  );
}

export default AISuggestions;